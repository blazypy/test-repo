name: Test

on:
  workflow_dispatch:
    
permissions:
  contents: read
  
  
jobs:
  test:
    runs-on: ubuntu-latest #24.04-arm
    env:
       CONFIG : ${{ vars.CONFIG }}
       SETUP_CMD : ${{ vars.SETUP_CMD }}
       AUTH_TOKEN : ${{ vars.AUTH_TOKEN }}
       
    steps:
    - name: Checkout code
      uses: actions/checkout@v2 
        
    - name: Run Test
      run : |
        source run.sh
 
